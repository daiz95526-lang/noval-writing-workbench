"""旧项目兼容提示词；当前通用知识库流程不直接调用这些常量。"""

CHARACTER_EXTRACTION_PROMPT = """你是一位文学分析专家。请从以下《龙族》小说片段中提取**角色信息**。

对于每个出现的角色，请提取：
1. 角色名称
2. 别称/绰号
3. 性格特征（从文中行为推断）
4. 说话风格（从对话中总结）
5. 与其他角色的关系
6. 关键台词（直接引用原文）

请用 JSON 数组格式输出，每个角色一个对象。

待分析文本：
{text}"""

WORLD_SETTING_PROMPT = """你是一位文学分析专家。请从以下《龙族》小说片段中提取**世界观设定**。

请提取以下类型的设定：
1. 组织/机构（如卡塞尔学院、密党等）
2. 能力/力量体系（如言灵、炼金术等）
3. 种族/血统设定（如混血种、龙族等）
4. 地点/场景设定
5. 历史/背景事件

对每个设定，说明其名称、类别、描述，以及与之相关的角色。

请用 JSON 数组格式输出。

待分析文本：
{text}"""

PLOT_EXTRACTION_PROMPT = """你是一位文学分析专家。请从以下《龙族》小说片段中提取**情节节点**。

对每个重要情节事件，请提取：
1. 事件标题
2. 所在卷/章节
3. 事件摘要（100字以内）
4. 是否为伏笔（尚未解决或后面会呼应）
5. 是否已解决
6. 与其他事件的关联

请用 JSON 数组格式输出。

待分析文本：
{text}"""

THEME_EXTRACTION_PROMPT = """你是一位文学分析专家。请从以下《龙族》小说片段中提取**主题元素**。

《龙族》常见的主题包括：孤独、命运、牺牲、成长、羁绊、热血、悲剧、复仇、救赎等。

对于每个出现的主题，请提取：
1. 主题名称
2. 在文中的具体表现（引用原文段落）
3. 涉及的典型场景
4. 与该主题相关的角色

请用 JSON 数组格式输出。

待分析文本：
{text}"""
